<UserControl x:Class="DNC.Views.ProgramListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DNC.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">


    
    <ListView ItemsSource="{Binding SelectedItem.ProgramList}">
        <ListView.View>
            <GridView>
                <GridViewColumn Header="No." DisplayMemberBinding="{Binding Number}"/>
                <GridViewColumn Header="Program Name" DisplayMemberBinding="{Binding Name}" />
            </GridView>
        </ListView.View>

        
    </ListView>

</UserControl>
