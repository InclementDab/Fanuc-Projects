<UserControl x:Class="DNC.Views.MachineListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:source="clr-namespace:DNC"
             xmlns:local="clr-namespace:DNC.Views"
             xmlns:model="clr-namespace:DNC.Models"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="300">



    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TreeView Grid.Row="0" Margin="5" ItemsSource="{Binding EnumeratedList, Mode=TwoWay}"
                                MinHeight="300"
                                AllowDrop="True"
                                SelectedItemChanged="TreeView_SelectedItemChanged">

            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type model:ModelBase}" ItemsSource="{Binding Children}">
                    <Border BorderBrush="Black" BorderThickness="0 0 0 0">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{Binding Icon}"/>

                            <Grid Width="auto" Height="auto">
                                <TextBlock Visibility="{Binding Path=IsNameEditing, Converter={StaticResource BoolToVisibilityConverter}}" Text="{Binding Name, Mode=OneWay}"/>
                                <TextBox Visibility="{Binding Path=IsNameEditing, Converter={StaticResource BoolToInvisibilityConverter}}" Text="{Binding Name}" KeyDown="TextBox_KeyDown" MouseDown="TextBox_MouseDown"/>
                            </Grid>

                            <StackPanel.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Command="Copy"/>
                                    <MenuItem Command="Cut"/>
                                    <MenuItem Command="Paste"/>
                                    <Separator/>
                                    <MenuItem Header="Rename"/>
                                    <MenuItem Header="Properties"/>
                                </ContextMenu>
                            </StackPanel.ContextMenu>
                        </StackPanel>
                    </Border>
                </HierarchicalDataTemplate>
            </TreeView.Resources>

        </TreeView>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Width="20" Height="20" Content="M" Command="{Binding AddMachineCommand}" Margin="2"/>
            <Button Width="20" Height="20" Content="F" Command="{Binding AddFolderCommand}" Margin="2"/>
        </StackPanel>
    </Grid>
</UserControl>
